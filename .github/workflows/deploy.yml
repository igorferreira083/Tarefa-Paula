
name: Deploy to Vercel

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install Vercel CLI
      run: npm i -g vercel

    - name: Deploy to Vercel
  run: vercel --token ${{ secrets.ThMRLYo6kqxr4ZotarlgqEug }} --org ${{ secrets.Giyt1QPpZm3PwQPKEYIqddjV }} --project ${{ secrets.prj_qvHa9Zx0ChkUaGjahZN8V3354vMZ }}

