
name: Deploy to Vercel

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install Vercel CLI
      run: npm i -g vercel

    - name: Deploy to Vercel
      run: vercel --token ${{ ThMRLYo6kqxr4ZotarlgqEug }} --prod # Se der erro, pode tentar colocar --yes
      env:
        VERCEL_ORG_ID: ${{ Giyt1QPpZm3PwQPKEYIqddjV }}
        VERCEL_PROJECT_ID: ${{  prj_qvHa9Zx0ChkUaGjahZN8V3354vMZ }}

